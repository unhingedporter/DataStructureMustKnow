# Find minimum number of coins needed to pay the bill
class CoinChange:

    def __init__(self):
        pass


# 10, 5, 2, 1
#
# 57
#
# 5 * 10 + 5 * 1 + 2 * 1 + 1 * 1


def findPossibleCombination(self, coin_list: [], total_sum):
    # coin_list = coin_list.sort()
    # count_of_coin = []
    # sum_needed = total_sum
    # for i in coin_list:
    #     if total_sum / coin_list[i] >= min(coin_list):
    #         total_sum
    #         coin_list[i]
    #
    #     sum_needed =
    #     if sum_needed % coin_list[i] > 0: sum_needed % coin_list[i]
    #     count_of_coin.append()
    #     if (== 0):
